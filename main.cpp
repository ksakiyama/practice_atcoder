#include <iostream>
#include <vector>
#include <algorithm>
#include <cmath>
#include <utility>

#define REP(i,n) for(int i=0;i<n;i++)

typedef long long ll;
using namespace std;

int main() {
  return 0;
}